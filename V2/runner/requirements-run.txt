# 측정 서버 전용 의존성.
#
# pyproject.toml은 datasets를 정규 의존성에 넣어 두었으나 그것을 쓰는 것은
# itembank.py 하나뿐이고, 하는 일은 MMLU-Pro 원본을 내려받아 후보 풀을
# 만드는 것이다. 문항 은행은 data/item_bank.json으로 이미 확정되었으므로
# 측정 서버에서는 그 경로가 다시 돌 일이 없다.
#
# datasets를 빼면 pyarrow와 그 의존 묶음이 따라오지 않는다. 1GB 인스턴스에서
# 설치 중 메모리가 몰리는 것을 피할 수 있고 디스크도 아낀다.
# 은행을 다시 만들 일이 생기면 그때는 로컬에서 requirements.txt로 돌린다.
requests>=2.31
python-dotenv>=1.0
